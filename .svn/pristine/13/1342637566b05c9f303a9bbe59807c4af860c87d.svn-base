import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SysSitemAllFieldsComponent } from './sys-sitem-all-fields.component';

describe('SysSitemAllFieldsComponent', () => {
  let component: SysSitemAllFieldsComponent;
  let fixture: ComponentFixture<SysSitemAllFieldsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SysSitemAllFieldsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SysSitemAllFieldsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
